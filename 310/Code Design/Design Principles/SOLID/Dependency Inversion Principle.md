Dependency should be on abstraction, not implementation.
- This means that we should not rely on the specific implementation details of a module when using it, we should be using the public methods it declares 
- High level modules should NOT depend on low level modules, both should depend on abstractions

#310